# Normalise line endings for cross-platform consistency
* text=auto

# Shell scripts must always use LF (CRLF breaks bash/zsh on WSL/Linux)
*.sh text eol=lf
Makefile text eol=lf
install.sh text eol=lf
